{
  "name": "photosync",
  "version": "1.0.0",
  "description": "Sync photos to AWS S3",
  "main": "backup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.320.0"
  },
  "devDependencies": {
    "grunt": "^1.0.3",
    "grunt-aws-s3": "^2.0.0",
    "mock-aws-s3": "^3.0.0"
  }
}
